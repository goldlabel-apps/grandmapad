{
  "name": "grandmapad",
  "packageManager": "yarn@4.3.1",
  "scripts": {
    "deploy": "yarn build && firebase deploy",
    "dev": "cd ./reactapp && yarn start",
    "build": "cd ./reactapp && yarn build",
    "test": "cd ./reactapp && yarn"
  }
}
